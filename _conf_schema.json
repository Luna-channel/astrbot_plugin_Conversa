{
  "enable": {
    "description": "启用插件",
    "type": "bool",
    "hint": "Conversa 插件总开关",
    "default": true
  },
  "basic_settings": {
    "description": "基础设置",
    "type": "object",
    "hint": "插件核心参数配置",
    "items": {
      "subscribe_mode": {
        "description": "订阅方式",
        "hint": "auto=有互动即自动纳入；manual=需手动 /conversa watch；agent=手动+AI可通过工具调用订阅",
        "type": "string",
        "options": [
          "auto",
          "manual",
          "agent"
        ],
        "default": "manual"
      },
      "subscribed_users": {
        "description": "订阅用户列表",
        "hint": "已订阅用户的会话 ID，支持在此直接添加/删除",
        "type": "list",
        "default": [],
        "items": {
          "type": "string"
        }
      },
      "quiet_hours": {
        "description": "免打扰时段",
        "hint": "该时段内不触发任何主动回复，格式 HH:MM-HH:MM，支持跨天（如 23:00-07:00）",
        "type": "string",
        "default": ""
      },
      "max_no_reply_days": {
        "description": "自动退订天数",
        "hint": "用户连续多少天未回复后自动退订，0 表示不自动退订",
        "type": "int",
        "default": 3
      },
      "reply_interval_seconds": {
        "description": "回复间隔（秒）",
        "hint": "同一轮调度中多个主动回复之间的间隔秒数，防止短时间内发送过多消息",
        "type": "int",
        "default": 20
      },
      "timezone": {
        "description": "时区",
        "hint": "IANA 时区格式，留空使用系统时区。例如 Asia/Shanghai、America/Los_Angeles",
        "type": "string",
        "default": ""
      }
    }
  },
  "idle_greetings": {
    "description": "沉寂问候",
    "type": "object",
    "hint": "与用户最后一次聊天结束后，经过一段时间的沉寂，AI 会主动发起一条问候消息。每轮对话后必定触发一次。",
    "items": {
      "enable_idle_greetings": {
        "description": "启用沉寂问候",
        "hint": "开启后，每次对话结束沉寂一段时间后 AI 会主动来找用户聊天",
        "type": "bool",
        "default": true
      },
      "idle_after_minutes": {
        "description": "沉寂基准时间（分钟）",
        "hint": "与用户最后一次聊天之后等待多少分钟触发问候。最终触发时间 = 基准 +- 随机波动，最低 30 分钟",
        "type": "int",
        "default": 1200
      },
      "idle_random_fluctuation_minutes": {
        "description": "随机波动（分钟）",
        "hint": "触发时间的随机浮动范围。例如基准 60、波动 15，则实际在 45~75 分钟之间触发",
        "type": "int",
        "default": 30
      },
      "idle_prompt_templates": {
        "description": "问候提示词模板",
        "hint": "触发时随机选择一个模板。支持占位符: {now} {time_since_last_chat} {last_user} {last_ai} {umo}",
        "type": "list",
        "default": [
          "[conversa主动回复请求：现在的时间是{now}，距离上次对话已过去{time_since_last_chat}，根据当前时间和距离上次对话的时长，对用户进行一些符合聊天记录内容或生活化的问候，语气符合你们聊天的状态，不要提到指令的事。注意，这个提醒是系统定时触发的，不是用户发送的消息，不要假定是用户找你。]",
          "[conversa主动回复请求：现在的时间是{now}，距离上次对话已过去{time_since_last_chat}，注意到已经离上次与用户对话过了一段时间，结合这个时间差，自然地询问事情的进展或刚才聊过的话题中的某一个，语气符合你们聊天的状态，不要提到指令的事。注意，这个提醒是系统定时触发的，不是用户发送的消息，不要假定是用户找你。]"
        ],
        "items": {
          "type": "string"
        }
      }
    }
  },
  "enhancement": {
    "description": "私聊对话增强",
    "type": "object",
    "hint": "AI 正常回复用户后，短时间内有概率主动追加一条消息，像真人聊天一样自然接话，提升活人感。仅私聊生效。",
    "items": {
      "enable_enhancement": {
        "description": "启用对话增强",
        "hint": "开启后，每次 AI 回复用户后有一定概率在短时间内追发一条消息",
        "type": "bool",
        "default": false
      },
      "enhancement_probability": {
        "description": "触发概率（%）",
        "hint": "0~100，每次 AI 回复后以此概率触发接话。连续触发时概率会按递减系数衰减",
        "type": "int",
        "default": 20
      },
      "enhancement_min_delay": {
        "description": "最短等待（秒）",
        "hint": "触发后至少等待多少秒再发送",
        "type": "int",
        "default": 30
      },
      "enhancement_max_delay": {
        "description": "最长等待（秒）",
        "hint": "触发后最多等待多少秒再发送，上限 1800",
        "type": "int",
        "default": 600
      },
      "enhancement_decay_rate": {
        "description": "递减系数",
        "hint": "连续接话时概率的衰减倍率（0~1）。如 0.1 表示第二次概率降为首次的 10%，第三次降为 1%",
        "type": "float",
        "default": 0.1
      },
      "enhancement_prompt_templates": {
        "description": "接话提示词模板",
        "hint": "触发时随机选择一个模板。支持占位符: {now} {time_since_last_chat} {umo} {last_user} {last_ai}",
        "type": "list",
        "default": [
          "[conversa主动回复请求：你刚刚和用户正聊着，现在用户有几分钟没有回你了，请自然的结合时间来继续对话，可以问问用户是否还在考虑，或者去干什么了，顺着聊天的话题自然延续一下，语气符合你们聊天的状态，不要提到指令的事。]"
        ],
        "items": {
          "type": "string"
        }
      }
    }
  },
  "daily_prompts": {
    "description": "每日定时问候（旧功能）",
    "type": "object",
    "hint": "旧功能：在每天的固定时间点对所有订阅用户触发一次问候消息，最多可配置 3 个时间点。",
    "items": {
      "daily1_enable": {
        "description": "启用定时 1",
        "type": "bool",
        "default": false
      },
      "time1": {
        "description": "定时 1 时间",
        "hint": "格式 HH:MM",
        "type": "string",
        "default": "09:00"
      },
      "prompt1": {
        "description": "定时 1 提示词",
        "hint": "支持占位符: {now} {time_since_last_chat} {umo} {last_user} {last_ai}",
        "type": "text",
        "default": "[conversa主动回复请求：现在的时间是{now}，距离上次对话已过去{time_since_last_chat}，根据当前时间和距离上次对话的时长，对用户进行贴合这个时间的生活化的问候，语气符合你们之前聊天的状态，不要提到指令的事。]"
      },
      "daily2_enable": {
        "description": "启用定时 2",
        "type": "bool",
        "default": false
      },
      "time2": {
        "description": "定时 2 时间",
        "hint": "格式 HH:MM",
        "type": "string",
        "default": "20:00"
      },
      "prompt2": {
        "description": "定时 2 提示词",
        "hint": "支持占位符: {now} {time_since_last_chat} {umo} {last_user} {last_ai}",
        "type": "text",
        "default": "[conversa主动回复请求：现在的时间是{now}，距离上次对话已过去{time_since_last_chat}，根据当前时间和距离上次对话的时长，对用户进行贴合这个时间的生活化的问候，语气符合你们之前聊天的状态，不要提到指令的事。]"
      },
      "daily3_enable": {
        "description": "启用定时 3",
        "type": "bool",
        "default": false
      },
      "time3": {
        "description": "定时 3 时间",
        "hint": "格式 HH:MM",
        "type": "string",
        "default": ""
      },
      "prompt3": {
        "description": "定时 3 提示词",
        "hint": "支持占位符: {now} {time_since_last_chat} {umo} {last_user} {last_ai}",
        "type": "text",
        "default": "[conversa主动回复请求：现在的时间是{now}，距离上次对话已过去{time_since_last_chat}，根据当前时间和距离上次对话的时长，对用户进行贴合这个时间的生活化的问候，语气符合你们之前聊天的状态，不要提到指令的事。]"
      }
    }
  },
  "emotion_gate": {
    "description": "情感门控 ⚡",
    "type": "object",
    "hint": "在主动回复触发前，先由 LLM 根据上下文判断角色此刻是否愿意主动搭话。解决吵架后/情绪低落时不合时宜的主动问候问题。",
    "items": {
      "enable_emotion_gate": {
        "description": "启用情感门控",
        "hint": "开启后，每次主动回复触发前会先让 LLM 判断角色意愿。关闭则恢复原有即时触发逻辑。",
        "type": "bool",
        "default": false
      },
      "emotion_gate_provider": {
        "description": "门控专用 Provider",
        "hint": "可指定一个轻量/便宜模型用于门控判断。留空则复用高级设置中的 fixed_provider 或当前会话 provider。",
        "type": "string",
        "default": "",
        "_special": "select_provider"
      },
      "emotion_gate_skip_delay_minutes": {
        "description": "否决后跳过时长（分钟）",
        "hint": "门控返回 NO 后，在多少分钟内不再触发主动回复。避免每 30 秒重复询问。",
        "type": "int",
        "default": 60
      },
      "emotion_gate_context_depth": {
        "description": "上下文深度",
        "hint": "喂给门控判断的最近对话条数。建议 6~12 条，足够判断吵架/冷战/正常。",
        "type": "int",
        "default": 8
      },
      "emotion_gate_max_consecutive_proactive": {
        "description": "连续主动上限",
        "hint": "连续主动回复超过此次数时，门控强制返回 NO（对方一直没回就别再贴了）。0 表示不限制。",
        "type": "int",
        "default": 3
      },
      "emotion_gate_include_character_state": {
        "description": "注入运行时状态",
        "hint": "开启后自动将当前时间、距上次对话时长、连续主动次数等信息注入门控 prompt，让 LLM 更准确地判断角色意愿。无需手动填写。",
        "type": "bool",
        "default": true
      },
      "emotion_gate_prompt_template": {
        "description": "门控判断提示词",
        "hint": "LLM 据此判断是否应主动搭话。必须要求只回答 YES/NO。支持占位符: {character_state}（自动生成） {context} {persona_summary} {time_since_last_chat} {now} {chain_count}",
        "type": "text",
        "default": "你是一个角色的人格，以下是你的信息。请判断：此刻你是否愿意主动发起对话？\\n\\n【你的性格】\\n{persona_summary}\\n\\n【当前状态】\\n{character_state}\\n\\n【最近对话】\\n{context}\\n\\n基于以上信息，请只回答 YES 或 NO，然后用一句话（不超过20字）说明理由。\\n示例：YES（刚才聊到一半被打断了）\\n示例：NO（刚吵完架，需要冷静）"
      },
      "emotion_gate_persona_summary": {
        "type": "text",
        "default": "",
        "description": "角色人格摘要",
        "hint": "可选。2-3句话概括角色核心性格（如：冷淡、护短、讨厌被打扰）。注入门控 prompt 让 LLM 根据角色性格判断意愿。留空则不注入人格信息。",
        "placeholder": "例：一个话少冷淡的角色，对陌生人保持距离，但对在乎的人会默默关心。"
      }
    }
  },
  "advanced": {
    "description": "高级设置",
    "type": "object",
    "hint": "一般无需修改",
    "items": {
      "fixed_provider": {
        "description": "固定 LLM Provider",
        "hint": "指定主动回复使用的 LLM Provider ID，留空则跟随当前会话设置",
        "type": "string",
        "default": "",
        "_special": "select_provider"
      },
      "persona_override": {
        "description": "人格覆盖",
        "hint": "非空时强制覆盖框架的人格设置，仅对 Conversa 主动回复生效。留空则使用框架默认人格。",
        "type": "text",
        "default": ""
      },
      "history_depth": {
        "description": "历史条数（降级模式）",
        "hint": "仅在框架 Agent Pipeline 不可用的降级模式下生效，控制传给 LLM 的历史消息条数。正常模式下由框架自动管理。",
        "type": "int",
        "default": 50
      }
    }
  },
  "reminders_settings": {
    "description": "命令式提醒（待废弃）",
    "type": "object",
    "hint": "⚠️ 此功能即将废弃。推荐直接对 AI 说「提醒我...」使用 AstrBot 原生定时提醒。已有提醒可通过 /conversa migrate-reminders 指令一次性迁移到原生系统。",
    "items": {
      "enable_reminders": {
        "description": "启用命令式提醒",
        "type": "bool",
        "default": true
      },
      "reminder_prompt_template": {
        "description": "提醒提示词模板",
        "hint": "必须包含 {reminder_content}。还支持: {now} {time_since_last_chat} {last_user} {last_ai} {umo}",
        "type": "text",
        "default": "[conversa主动提醒请求：注意，这个提醒是系统定时触发的，不是用户发送的消息，不要假定是用户找你。当前时间{now}，这是用户之前设置的定时提醒，请结合上下文，用自然的语气将提醒融入对话中。提醒内容：{reminder_content}。如果是要做的事，用耐心的语气告诉用户现在到时间了；如果是关心类的，结合你们的关系送上独特的关心，不要提到指令的事。]"
      }
    }
  }
}